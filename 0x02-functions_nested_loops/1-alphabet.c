#include "main.h"

/**
 * print_alphabet - Prints the alphabet in lowercase.
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
	int ch = 'a';
	while (ch <= 'z')
	{
		_putchar(i);
	}
	_putchar('\n');
}
